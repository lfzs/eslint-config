# eslint 配置文件

## 使用方式

### 原生 JavaScript 项目

1、`pnpm add @jzfe/eslint-config-vanilla -D`

2、添加到 `eslint.config.js` 配置文件

### Vue 3 项目

1、`pnpm add @jzfe/eslint-config-vue -D`

2、添加到 `eslint.config.js` 配置文件
